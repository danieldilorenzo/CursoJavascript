function parimp(n) {
  if (n % 2 == 0) {
    return "Par!";
  } else {
    return "Impar!";
  }
}

let res = parimp(9);
console.log(res);

//Criada função que verifica se um número é par ou impar baseado no retorno dele
//Criada uma variável que define o valor de n como 9
//Imprimi a variável, retornando se o número é par ou impar

//Poderia também fazer

console.log(parimp(12));
